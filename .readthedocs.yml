# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Set the version of Python and other tools you might need
build:
  os: ubuntu-20.04
  tools:
    python: "3.12"
    # You can also specify other tool versions:
    # nodejs: "16"
    # rust: "1.55"
    # golang: "1.17"
  jobs:
    post_build:
      # Rebuild the chatbot search index from the just-built QSDsan HTML.
      # Skips gracefully when VOYAGE_API_KEY is not set so docs builds and PR
      # previews never fail just because the embedding key is absent.
      - |
        if [ -n "$VOYAGE_API_KEY" ]; then
          pip install voyageai beautifulsoup4 &&
          python docs/chatbot/index_docs.py
        else
          echo "VOYAGE_API_KEY not set; skipping chatbot reindex."
        fi

# Build documentation in the docs/ directory with Sphinx
sphinx:
   configuration: docs/source/conf.py

# If using Sphinx, optionally build your docs in additional formats such as PDF
# formats:
#    - pdf

# Optionally declare the Python requirements required to build your docs
python:
   install:
   - method: pip
     path: .
     extra_requirements:
     - docs
